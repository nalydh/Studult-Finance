# Instructions
1. Run the docker container for the database
2. Run the front-end via `npm run dev`
3. Run the back-end via `uvicorn app.main:app --reload`
- Back-end must be launched from the virtual environment by doing `source venv/bin/activate`